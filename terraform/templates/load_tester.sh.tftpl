#!/bin/bash
set -euxo pipefail

dnf update -y
dnf install -y docker git jq
systemctl enable --now docker
usermod -aG docker ec2-user || true

echo "${project_name} load tester host is ready." >/etc/motd
